Round user avatar — initials on a deterministic warm color, or an image.

```jsx
<Avatar name="Dana Reyes" role="Agent" />
<Avatar name="Sam Okafor" size={28} />
```

The background color is hashed from the name, so a given person is always the same color. `role="Agent"` adds a terracotta presence dot.
