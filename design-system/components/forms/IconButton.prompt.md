Square single-icon button for toolbars and row actions — always give it a `label` for accessibility.

```jsx
<IconButton icon="more-horizontal" label="More actions" />
<IconButton icon="check" variant="soft" label="Resolve" />
```

Variants: `ghost` (default), `secondary`, `soft`, `primary`. Sizes `sm`/`md`/`lg`.
