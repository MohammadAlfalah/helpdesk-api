Single-line text field with label, hint/error text, and optional Lucide icon.

```jsx
<Input label="Search tickets" icon="search" placeholder="Search title or description…" />
<Input label="Email" error="Enter a valid email" defaultValue="bad@" />
```

Focus shows a terracotta ring. Pass `error` to mark the field invalid.
