One-liner: on/off toggle for binary settings (notification prefs, internal-note mode, filter toggles).

```jsx
<Switch label="Email me on assignment" checked={v} onChange={...} />
```

Props: `label`, `checked`, `disabled`, native input attrs. Bouncy knob transition.
