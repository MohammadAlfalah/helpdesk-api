The base surface — white, rounded (14px), soft warm shadow. Most things sit on a Card.

```jsx
<Card>Ticket summary…</Card>
<Card interactive elevation="sm" onClick={open}>Clickable ticket row</Card>
```

`interactive` adds a hover lift. `elevation`: `none` / `sm` / `md` / `lg`.
