{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Jwt": {
    "_comment": "Key is intentionally empty here. Provide it at runtime via the Jwt__Key environment variable (see docker-compose.yml / render.yaml) or user-secrets. For local dev it is set in appsettings.Development.json. The app refuses to start without a key of at least 32 bytes.",
    "Key": "",
    "Issuer": "HelpDeskApi",
    "Audience": "HelpDeskClient"
  },
  "Sla": {
    "UrgentHours": 1,
    "HighHours": 4,
    "MediumHours": 24,
    "LowHours": 72,
    "CheckIntervalSeconds": 60
  }
}
